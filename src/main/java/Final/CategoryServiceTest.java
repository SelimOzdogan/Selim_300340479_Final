package Final;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;

class CategoryServiceTest {
//    SavingService obj = new SavingService();

    @BeforeEach
    public void initEach() {
//        List<Saving> Categorylist = new ArrayList<>();
//        Categorylist.add(new Saving("catcode1", "catdesc1"));
//        Categorylist.add(new Saving("catcode2", "catdesc2"));
//        SavingService.setTodos(Categorylist);
    }

    @Test
    void retrieveTodos() {
//        List<Saving> Categorylist = obj.retrieveTodos();
//
//        assertEquals(2, Categorylist.size());
//        assertEquals("catcode1", Categorylist.get(0).getCatcode());
//        assertEquals("catdesc1", Categorylist.get(0).getCatdesc());
//        assertEquals("catcode2", Categorylist.get(1).getCatcode());
//        assertEquals("catdesc2", Categorylist.get(1).getCatdesc());
    }

    @Test
    void addTodo() {
//        obj.addTodo("catcode3", "catdesc3");
//        List<Saving> Categorylist = obj.retrieveTodos();
//
//        assertEquals(3, Categorylist.size());
//        assertEquals("catcode3", Categorylist.get(2).getCatcode());
//        assertEquals("catdesc3", Categorylist.get(2).getCatdesc());
    }

    @Test
    void deleteTodo() {
//        obj.deleteTodo("catcode2");
//        List<Saving> Categorylist = obj.retrieveTodos();
//
//        assertEquals(1, Categorylist.size());
//        assertEquals("catcode1", Categorylist.get(0).getCatcode());
//        assertEquals("catdesc1", Categorylist.get(0).getCatdesc());
    }
}